import moment from 'moment;'
import { pipe } from "./helpers";

export const yearsMonthsHoursSince = (date) => pipe(yearsSince, monthsSince, hoursSince)(date);

export const yearsSince = (date) => {

};

export const monthsSince = (date) => {

};

export const hoursSince= (date) => {

};
